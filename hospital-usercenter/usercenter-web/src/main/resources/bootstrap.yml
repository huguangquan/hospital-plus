spring:
  application:
    name: hospital-usercenter

  cloud:
    nacos:
      namespace: 07bd59f4-f242-4e00-943f-a5c5e1f480f9
      server-addr: http://127.0.0.1:8848
      username: nacos
      password: nacos
      config:
        prefix: ${spring.application.name}
        context-path: nacos
        file-extension: yml
        namespace: ${spring.cloud.nacos.namespace}
        refresh-enabled: true
      discovery:
        namespace: ${spring.cloud.nacos.namespace}
